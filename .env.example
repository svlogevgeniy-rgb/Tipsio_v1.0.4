# Database (used by Prisma + docker-compose.local)
# Keep DATABASE_URL in sync with the DB_* values below
DB_HOST="localhost"
DB_PORT="5432"
DB_USER="tipsio"
DB_PASSWORD="postgres"
DB_NAME="tipsio"
DATABASE_URL="postgresql://tipsio:postgres@localhost:5432/tipsio?schema=public"

# NextAuth - GENERATE NEW SECRET FOR PRODUCTION
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="CHANGE_ME_IN_PRODUCTION"
NEXTAUTH_URL="http://localhost:3000"

# Midtrans Payment Gateway
MIDTRANS_SERVER_KEY=""
MIDTRANS_CLIENT_KEY=""
MIDTRANS_IS_PRODUCTION="false"

# Email (Resend)
RESEND_API_KEY=""

# SMS (Twilio)
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_PHONE_NUMBER=""

# Encryption - GENERATE NEW KEY FOR PRODUCTION
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY="CHANGE_ME_IN_PRODUCTION"
