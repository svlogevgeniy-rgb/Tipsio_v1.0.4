# Объяснение "замков" в таблице

## Возможные варианты:

### 1. Визуальные иконки замков в UI
Если вы видите иконки замков 🔒 в интерфейсе, это обычно означает:
- **Read-only поля** - поля, которые нельзя редактировать
- **Disabled поля** - поля, которые временно заблокированы
- **Защищенные данные** - данные, требующие особых прав доступа

### 2. Блокировки в базе данных PostgreSQL
PostgreSQL может блокировать таблицы при:
- Длительных транзакциях
- Миграциях базы данных
- Одновременных операциях записи

**Проверить блокировки:**
```sql
-- Текущие блокировки
SELECT 
    locktype,
    relation::regclass,
    mode,
    granted
FROM pg_locks
WHERE relation IS NOT NULL;
```

### 3. Статус "BLOCKED" у заведений
В системе есть статус `BLOCKED` для заведений:
- Заведения могут быть заблокированы администратором
- Блокированные заведения не могут принимать чаевые

**Проверить заблокированные заведения:**
```sql
SELECT id, name, status
FROM "Venue"
WHERE status = 'BLOCKED';
```

### 4. Readonly поля в форме профиля
В форме профиля некоторые поля могут быть readonly:
- Email - может быть защищен от изменения (зависит от логики авторизации)
- ID - всегда readonly (техническое поле)

## Решение проблем:

### Если поля заблокированы в UI:
1. Проверьте права доступа пользователя
2. Убедитесь, что форма не в состоянии загрузки (`isLoading`)
3. Проверьте, не установлен ли атрибут `disabled` или `readOnly`

### Если таблицы заблокированы в БД:
1. Проверьте активные транзакции
2. Завершите долгие запросы
3. Перезапустите миграции, если они зависли

### Если заведение заблокировано:
1. Проверьте статус в админ-панели
2. Разблокируйте через админ-панель или SQL:
```sql
UPDATE "Venue"
SET status = 'ACTIVE'
WHERE id = 'venue-id-here';
```

