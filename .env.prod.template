# ============================================
# TIPSIO PRODUCTION ENVIRONMENT
# ============================================
# Скопируйте этот файл в .env и заполните значения
# cp .env.prod.template .env
#
# Генерация секретов: ./generate-secrets.sh
# ============================================

# ============================================
# DATABASE (PostgreSQL 15+)
# ============================================
DB_USER=tipsio
DB_PASSWORD=<СГЕНЕРИРОВАТЬ: openssl rand -base64 24>
DB_NAME=tipsio
# DATABASE_URL формируется автоматически в docker-compose
# Для локальной разработки: DATABASE_URL=postgresql://tipsio:password@localhost:5432/tipsio

# ============================================
# NEXTAUTH v5
# ============================================
# Секрет для подписи JWT токенов (обязательно уникальный для каждого окружения!)
NEXTAUTH_SECRET=<СГЕНЕРИРОВАТЬ: openssl rand -base64 32>

# URL приложения (с https:// для production)
NEXTAUTH_URL=https://YOUR_DOMAIN.com

# ============================================
# ENCRYPTION (256-bit AES key)
# ============================================
# Используется для шифрования чувствительных данных (Midtrans keys и т.д.)
ENCRYPTION_KEY=<СГЕНЕРИРОВАТЬ: openssl rand -hex 32>

# ============================================
# MIDTRANS PAYMENT GATEWAY
# ============================================
# Получить в Midtrans Dashboard: https://dashboard.midtrans.com
# Settings -> Access Keys

# Server Key (для backend API calls)
MIDTRANS_SERVER_KEY=<ВАШ_SERVER_KEY_ИЗ_MIDTRANS>

# Client Key (для frontend Snap.js)
MIDTRANS_CLIENT_KEY=<ВАШ_CLIENT_KEY_ИЗ_MIDTRANS>

# Режим: true = production, false = sandbox
MIDTRANS_IS_PRODUCTION=true

# ============================================
# EMAIL (Resend) - ОПЦИОНАЛЬНО
# ============================================
# Для отправки email уведомлений
# Получить на: https://resend.com/api-keys
RESEND_API_KEY=

# ============================================
# SMS (Twilio) - ОПЦИОНАЛЬНО
# ============================================
# Для OTP через SMS
# Получить на: https://console.twilio.com
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# ============================================
# ПРИМЕЧАНИЯ
# ============================================
# 1. НЕ коммитьте этот файл в git!
# 2. Секреты должны быть уникальны для каждого окружения
# 3. Для sandbox Midtrans используйте sandbox ключи
# 4. NEXTAUTH_URL должен точно совпадать с доменом (включая https://)
